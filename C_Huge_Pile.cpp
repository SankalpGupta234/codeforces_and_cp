#include <bits/stdc++.h>

using namespace std;

int main()
{
    // Fast IO
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // Code below

    int t{};
    cin >> t;

    for (int i = 0; i < t; i++)
    {
        int n{}, k{};
        cin >> n >> k;

        int time = 0;

        while (n > 0)
        {
            
        }

    }

    return 0;
}